{% if title is defined and title is not empty %}
<h1 class="mb-6 whatwedo-utility-heading-1">{{ title | trans }}{% if view.data is not null and view.data.id is not null %}: {{ view.data | wwd_crud_to_string }}{% endif %}</h1>
{% endif %}
